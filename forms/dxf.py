def load (file):
    # Loading data from file
    def structurize ():
        pass

def gen (kind, params, layer='0', colour='0'):
    # Generation of dxf-objects in list-style
    def poly (params, layer='0'):
        if isinstance(params, Exponion):
            # Polyline generation from Exponion
            pass
        if isinstance(params, dict):
            pass
        pass
    def solid (params, layer='0'):
        if isinstance(params, Exponion):
            pass
        if isinstance(params, dict):
            pass
        pass
    def rect (params, layer='0'):
        pass
    def line (params, layer='0'):
        pass
    def vertex (x, y, a, layer='0'):
        pass
    def circle (x, y, r, layer='0'):
        pass
    def arc (p1, p2, a, layer='0'):
        pass

def create (stack):
    # Parcing dxf-list to text
    pass
